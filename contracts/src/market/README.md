# v1 core market — grant-scope, not PoC

The RFP's baseline required scope (listings, offers, purchases, new registrations, bulk
tools, OpenSea aggregation) settles on Seaport, following the same proven pattern as the
reference architecture we studied. None of this ships as part of the PoC — see
`docs/roadmap.md` for sequencing. This folder is a placeholder marker so the repo layout
communicates where that work lands.
