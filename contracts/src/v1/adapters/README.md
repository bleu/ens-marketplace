# Swap adapter implementations

Empty by design. The swap/intent provider for Slice 2 is a deliberate open decision — see
`docs/slice-2.md` and `docs/roadmap.md`. The first concrete implementation of
`ISwapAdapter` (CoW Protocol is the strongest internal candidate) lands here once chosen.
