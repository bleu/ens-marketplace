# apps/api's own database (Grails listings)
API_PG_USER=postgres
API_PG_PASSWORD=
API_PG_DATABASE=ens_marketplace_api

# Host port apps/api is published on — Caddy reverse-proxies to this
API_EXPOSED_PORT=41000
